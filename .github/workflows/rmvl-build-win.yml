name: Windows support for RMVL

on:
  workflow_call:

jobs:
  build_and_test:
    name: Build and test on Windows
    runs-on: windows-2022
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Configure RMVL by CMake 
      run: cmake -B ${{ github.workspace }}/build -DBUILD_OPEN62541=ON -DBUILD_TESTS=ON

    - name: Build RMVL
      run: cmake --build ${{ github.workspace }}/build --parallel 4 --config Release

    - name: Test RMVL
      run: ctest --test-dir ${{ github.workspace }}/build

    - name: Install RMVL
      run: cmake --install ${{ github.workspace }}/build

    # - name: (Deploy test) Checkout repository
    #   uses: actions/checkout@v4
    #   with:
    #     repository: cv-rmvl/ci-workflow
    #     path: deploy-repo

    # - name: (Deploy test) Test
    #   run: deploy-repo/deploy/scripts/build.bat
